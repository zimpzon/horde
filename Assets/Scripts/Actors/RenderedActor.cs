using UnityEngine;

public class RenderedActor : MonoBehaviour {

    public struct LightData
    {
        Color Color;
        float Size;
        bool Enabled;
    }

    public struct DropshadowData
    {
        float Size;
        bool Enabled;
    }

    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
