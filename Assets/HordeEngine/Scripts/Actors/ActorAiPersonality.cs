using UnityEngine;

// Use a ScriptableObject to override selected curves. Can clamp min and max here.

public class ActorAiPersonality : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
